<!-- Ask why this div was necessary, with positioning system
pulling things down -->


What is up <%= @user.username %> ?

<script type="text/javascript">

<% content = render(:partial => "rabl_partials/annotation",
:locals => {:type => :collection,
						:annotation => @user.annotations} ).html_safe %>

RapGenius.Repositories.UserAnnotationStreams =
JSON.parse("<%="#{j content}".html_safe %>");

console.log(RapGenius.Repositories.UserAnnotationStreams.constructor);

RapGenius.Repositories.UserSongStreams =
<%= @user.songs.to_json.html_safe %>;

var activityView = new RapGenius.Views.UserActivityView;

$("#body").append(activityView.render().$el);

</script>
